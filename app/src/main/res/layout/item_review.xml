<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="ua.meugen.android.popularmovies.viewmodel.MovieReviewItemViewModel"/>
        <variable name="model" type="MovieReviewItemViewModel"/>
    </data>

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="?android:attr/listPreferredItemHeight"
        android:padding="5dp"
        android:background="@drawable/button_background"
        android:onClick="@{model}">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:maxLines="1"
            android:ellipsize="end"
            android:gravity="center_vertical"
            android:layout_weight="1"
            tools:text="Very good movie 9.5/10"
            android:id="@+id/content"
            android:textAppearance="?android:attr/textAppearanceMedium"
            android:text="@{model.review.content}"/>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:text="elshaarawy"
            android:textAppearance="?android:attr/textAppearanceSmall"
            android:gravity="end"
            android:id="@+id/author"
            android:text="@{model.review.author}"/>

    </LinearLayout>
</layout>