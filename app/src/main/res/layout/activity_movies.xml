<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="ua.meugen.android.popularmovies.viewmodel.MoviesViewModel"/>
        <variable name="model" type="MoviesViewModel"/>
    </data>

    <android.support.v4.widget.SwipeRefreshLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="ua.meugen.android.popularmovies.view.activities.MoviesActivity"
        android:id="@+id/swipe_refresh"
        app:onRefreshListener="@{model}"
        app:refreshing="@{model.refreshing}">

        <android.support.v7.widget.RecyclerView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/recycler"
            app:layoutManager="GridLayoutManager"
            app:spanCount="@integer/main_recycler_span_count"
            app:adapter="@{model.adapter}"/>

    </android.support.v4.widget.SwipeRefreshLayout>
</layout>